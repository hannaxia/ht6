{
  "name": "innsight",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "web",
    "api",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n ml,api,web -c magenta,blue,green \"npm run dev:ml\" \"npm run dev -w api\" \"npm run dev -w web\"",
    "dev:ml": ".venv\\Scripts\\python.exe -m uvicorn main:app --app-dir ml/service --port 8000",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:prop": "npm run test:prop --workspaces --if-present",
    "test:int": "npm run test:int --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "mongodb": "^7.5.0",
    "three": "^0.185.1"
  }
}
